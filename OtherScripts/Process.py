def processMatrix(measure, matrix):
        if 'rmsd' in measure:
                #
        elif 'seq_id' in measure:
                #
        elif 'gdt' in measure:
                #
        